// Lots more testing should go in here.  
// Note to self:  incorporate nock.  See https://semaphoreci.com/community/tutorials/mocking-external-http-requests-in-node-tests-with-nock

const expect = require('chai').expect;

describe('First test', () => {
    it('Should assert true to be true', () => {
        expect(true).to.be.true;
    });
}); 